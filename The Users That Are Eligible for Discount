A user is eligible for a discount if they had a purchase in the inclusive interval of time
[2022-03-08, 2022-03-20] with at least minAmount amount,1000. Write an SQL query to report the IDs of the users that are eligible for a discount.

Return the result table ordered by user_id.

Table: Purchases

Query : - 

SELECT user_id
FROM purchases
WHERE time_stamp between '2022-03-08' AND '2022-03-20' AND amount >= 1000
ORDER BY user_id ;
